# Memory index

- [Scroll restoration and the detached container](scroll-restoration-detached-container.md) — never read scrollTop in an unmount cleanup; verify 17.8 by driving a real browser, not by reading code.
- [Screenshot harness side effects](shots-harness-side-effects.md) — a reused Chromium profile serves a stale bundle, and rendering the matrix writes a real expense into the live database.

Note: this project's older frontend memories live in the repo-root
`.claude/agent-memory/implementer-frontend/` (Autonom-OS context, forcing
interactive states, the Chromium sandbox, the needs-input affordance, building
mockups on Run 01's stylesheet). Read both directories.
